# 方案二完整设计：NovelWriter 思路 + PenShot + 自研视频 Provider

这个方案的核心不是把两个仓库直接复制到一起，而是：

> **用统一的 Python + LangGraph 架构重新组织整个项目，复用 PenShot 的分镜能力，借鉴 NovelWriter 的故事生成流程。**

最终产品流程：

```text
用户输入简单灵感
        ↓
AI故事创作系统
        ↓
短漫剧故事
        ↓
漫剧改编系统
        ↓
结构化视频剧本
        ↓
PenShot分镜系统
        ↓
镜头Prompt
        ↓
图片/视频厂商API
        ↓
视频片段
        ↓
FFmpeg合成
        ↓
最终漫剧
```

NovelWriter 当前是 Python + Tkinter 项目，已经包含世界观生成、故事结构、大纲、场景规划、正文生成和多层审查思路；PenShot 则是 Python 3.10+、LangChain、LangGraph 技术路线，支持 Python SDK、REST API、LangGraph节点和结构化分镜输出。二者在语言层面比较匹配。([GitHub][1])

---

# 一、项目最终定位

建议项目名称：

> **StoryMotion：从灵感到AI漫剧的多智能体创作平台**

一句话介绍：

> 用户输入小说风格、角色姓名、世界观和关键脑洞，多个AI Agent协作完成故事规划、内容续写、漫剧改编、分镜设计和视频生成。

系统分为四层：

```text
第一层：Story Creation
灵感 → 故事

第二层：Screenplay Adaptation
故事 → 漫剧剧本

第三层：Storyboard Generation
剧本 → 分镜和视频Prompt

第四层：Media Generation
Prompt → 图片、视频、成片
```

课程重点放在前三层，第四层只是调用厂商API完成展示。

---

# 二、为什么不直接合并两个源码仓库

NovelWriter虽然有比较完整的小说创作流程，但其现有界面基于 Tkinter，内部包含自己的文件管理和生成逻辑。它的 Agent 化设计也采用“现有应用与 Agent 层分离”的思路，包括质量控制、连续性检查、规划和反馈循环。([GitHub][1])

PenShot则已经是相对标准的 Python 包：

```text
src/penshot
examples
tests
Dockerfile
pyproject.toml
REST API
Python SDK
LangGraph Nodes
```

它更适合被你们作为依赖调用。([GitHub][2])

因此推荐：

```text
NovelWriter：
参考架构、Prompt、生成步骤
不直接搬Tkinter界面

PenShot：
安装为Python依赖
直接调用SDK或LangGraph节点
```

换句话说：

> NovelWriter是“设计参考”，PenShot是“可复用组件”。

这样项目结构更干净，也更能说明哪些功能是你们自己实现的。

---

# 三、完整系统架构

```text
┌─────────────────────────────────────┐
│            Web前端                  │
│ Streamlit 或 React                  │
│                                     │
│ 灵感输入、故事编辑、分镜预览、生成  │
└─────────────────┬───────────────────┘
                  │ REST API / WebSocket
                  ▼
┌─────────────────────────────────────┐
│          FastAPI Backend            │
│                                     │
│ 项目管理、任务状态、文件管理        │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│       LangGraph Orchestrator        │
│                                     │
│ Story Graph                         │
│ Adaptation Graph                    │
│ Storyboard Graph                    │
│ Media Graph                         │
└───────┬──────────────┬──────────────┘
        │              │
        ▼              ▼
┌──────────────┐ ┌────────────────────┐
│故事生成Agents│ │ PenShot SDK/Nodes  │
│自研          │ │ 剧本→分镜→Prompt   │
└──────────────┘ └────────────────────┘
        │              │
        └──────┬───────┘
               ▼
┌─────────────────────────────────────┐
│          Provider Layer             │
│                                     │
│ ImageProvider                       │
│ VideoProvider                       │
│ TTSProvider                         │
└─────────────────┬───────────────────┘
                  ▼
┌─────────────────────────────────────┐
│ 文件系统 / SQLite / Chroma          │
│ FFmpeg                              │
└─────────────────────────────────────┘
```

LangGraph本身适合管理长时间、有状态、存在循环和人工干预的工作流，支持持久化、状态管理和 human-in-the-loop；这正好适合“生成—审核—修改—继续”的故事生产流程。([Docs by LangChain][3])

---

# 四、系统中的四个主要工作流

## 工作流一：灵感生成故事

输入：

```json
{
  "genre": "东方玄幻",
  "style": "热血爽文",
  "protagonist_name": "林辰",
  "protagonist_trait": "表面冷静，实际重感情",
  "core_idea": "主角每天可以让时间倒退十秒",
  "target_duration": 60,
  "ending_type": "悬念",
  "additional_requirements": [
    "角色不超过3人",
    "场景不超过5个",
    "适合竖屏漫剧"
  ]
}
```

LangGraph流程：

```text
START
  ↓
Requirement Agent
  ↓
World Builder Agent
  ↓
Character Agent
  ↓
Plot Planner Agent
  ↓
Story Writer Agent
  ↓
Consistency Reviewer
  ↓
质量是否通过？
  ├── 否 → Revision Agent → 再审核
  └── 是 → 人工确认
                    ↓
                   END
```

---

## 工作流二：故事改编成漫剧剧本

```text
完整故事
  ↓
Duration Planner
  ↓
Screenplay Adapter
  ↓
Dialogue Agent
  ↓
Visualizability Reviewer
  ↓
结构化剧本
```

这一层非常重要。

因为小说故事和视频剧本不是同一种文本。

小说可以写：

> 林辰心中涌起一阵复杂的情绪，他想起了三年前被逐出宗门的那个雨夜。

视频剧本要改成：

```text
场景：宗门广场，雨夜

画面：
少年林辰跪在石阶上，衣服被雨水浸透。
长老将宗门令牌扔到他的面前。

长老：
“从今天起，你不再是玄天宗弟子。”

旁白：
“三年前，他失去了一切。”
```

所以中间必须有专门的 `Screenplay Adapter Agent`。

---

## 工作流三：剧本生成分镜

这一层使用 PenShot。

PenShot本身的流程包括：

```text
剧本解析
  ↓
场景识别
  ↓
时间规划
  ↓
镜头切分
  ↓
视频Prompt生成
  ↓
连续性检查
  ↓
结构化JSON输出
```

它支持长文本切分、角色和场景连续性管理、不同视频模型Prompt适配，以及中文和英文视觉描述。([GitHub][2])

你们的主图可以将 PenShot 封装为一个节点：

```text
screenplay_adapter
        ↓
penshot_storyboard
        ↓
storyboard_reviewer
```

---

## 工作流四：分镜生成视频

```text
分镜JSON
  ↓
角色参考图生成
  ↓
场景参考图生成
  ↓
首帧图片生成
  ↓
视频生成任务提交
  ↓
轮询任务状态
  ↓
下载视频片段
  ↓
FFmpeg合成
```

两周内建议只接：

* 一家图片生成厂商
* 一家视频生成厂商
* 可选一家语音厂商

不要一开始就接五六个平台。

---

# 五、Agent详细职责

## 1. Requirement Agent

负责将用户的零散灵感转换为明确约束。

输入：

```text
我想写一个修仙故事，主角叫林辰，
他能让时间倒退十秒，整体要热血一点。
```

输出：

```json
{
  "genre": "东方玄幻",
  "tone": ["热血", "悬疑"],
  "protagonist": {
    "name": "林辰",
    "ability": "每天可将时间倒退十秒",
    "personality": "待补充"
  },
  "target_duration": 60,
  "constraints": {
    "max_characters": 3,
    "max_locations": 5,
    "ending": "cliffhanger"
  },
  "missing_fields": [
    "反派设定",
    "主角目标"
  ]
}
```

第一版不必强迫用户补充缺失项，可以让 Agent 自动合理补全。

---

## 2. World Builder Agent

负责：

* 世界类型
* 能力体系
* 社会规则
* 关键禁忌
* 能力使用限制

输出不要太长。

```json
{
  "world_name": "九域",
  "era": "宗门统治时代",
  "power_system": "灵脉修炼",
  "special_rule": "时间类能力会引发天道反噬",
  "locations": [
    "玄天宗",
    "废弃试炼塔"
  ]
}
```

对于60秒漫剧，只需要生成与当前剧情直接相关的世界观。

不要生成几千字背景资料。

---

## 3. Character Agent

生成角色卡：

```json
{
  "characters": [
    {
      "id": "char_001",
      "name": "林辰",
      "role": "protagonist",
      "age": 18,
      "appearance": {
        "hair": "黑色短发",
        "clothing": "深灰色宗门服",
        "distinctive_feature": "左眼下方有浅色伤痕"
      },
      "personality": [
        "冷静",
        "执着",
        "重感情"
      ],
      "goal": "查明兄长死亡真相",
      "ability": "每天让时间倒退十秒",
      "visual_prompt": "..."
    }
  ]
}
```

角色卡将同时提供给：

* 故事生成 Agent
* 分镜 Agent
* 图片生成 Provider
* 视频生成 Provider

这是保证角色一致性的基础。

---

## 4. Plot Planner Agent

不直接写正文，而是先生成情节节拍。

推荐使用五段式短剧结构：

```text
1. Hook
2. Setup
3. Conflict
4. Reversal
5. Cliffhanger
```

例如：

```json
{
  "beats": [
    {
      "type": "hook",
      "duration": 5,
      "content": "林辰被妖兽击中，却突然回到十秒前"
    },
    {
      "type": "setup",
      "duration": 10,
      "content": "他发现自己觉醒了时间回溯能力"
    },
    {
      "type": "conflict",
      "duration": 20,
      "content": "他不断回溯躲避攻击，但每次都会出现黑色裂纹"
    },
    {
      "type": "reversal",
      "duration": 15,
      "content": "妖兽突然开口，说它认识林辰的兄长"
    },
    {
      "type": "cliffhanger",
      "duration": 10,
      "content": "妖兽说：你的兄长并没有死"
    }
  ]
}
```

---

## 5. Story Writer Agent

根据前面所有结构生成短篇故事。

要求：

* 500至1000字
* 重点描写当前事件
* 不扩展过多支线
* 不增加未经批准的重要角色
* 保留可视化动作
* 避免大量抽象心理描写

---

## 6. Consistency Reviewer

负责检查：

```json
{
  "score": 82,
  "passed": false,
  "issues": [
    {
      "type": "character_conflict",
      "description": "角色卡规定林辰不会剑术，但正文中使用了御剑术。",
      "severity": "high",
      "suggestion": "改为使用闪避或时间回溯逃脱。"
    }
  ]
}
```

检查维度：

| 维度     | 检查内容          |
| ------ | ------------- |
| 人物一致性  | 性格、能力、外观是否冲突  |
| 世界观一致性 | 能力规则是否被违反     |
| 情节逻辑   | 因果是否完整        |
| 风格一致性  | 是否符合热血、悬疑等要求  |
| 时长适配   | 内容是否能在目标时间内表达 |
| 角色数量   | 是否超出限制        |
| 场景数量   | 是否超出限制        |

最多返工两次，避免无限循环。

---

## 7. Screenplay Adapter Agent

把故事变成场景剧本。

输出：

```json
{
  "scenes": [
    {
      "scene_id": "scene_001",
      "location_id": "location_001",
      "time": "夜晚",
      "duration": 8,
      "characters": ["char_001", "char_002"],
      "scene_goal": "展示主角首次使用能力",
      "action": "妖兽利爪刺向林辰胸口，画面突然倒放",
      "dialogues": [],
      "voiceover": "就在死亡降临的一刻，时间倒退了十秒。",
      "emotion": "震惊",
      "transition": "flash"
    }
  ]
}
```

---

## 8. Visualizability Reviewer

判断每一句能不能被镜头表现。

例如下面这种内容不适合直接生成视频：

> 林辰隐约意识到，这或许是命运对他的某种暗示。

审核 Agent 应改成：

```text
林辰低头看向手背。
一道黑色时钟纹路缓缓亮起。
```

审核规则：

* 抽象心理转成表情、动作或旁白
* 模糊描述转成明确画面
* 一个镜头只保留一个核心动作
* 避免一个镜头出现太多角色
* 避免无法生成的复杂物理变化

---

# 六、核心数据结构

整个系统能不能顺利集成，主要取决于数据结构，而不是 Prompt。

建议使用 Pydantic。

## ProjectBrief

```python
from pydantic import BaseModel, Field


class ProjectBrief(BaseModel):
    genre: str
    style: list[str]
    protagonist_name: str
    core_idea: str
    target_duration: int = Field(default=60, ge=15, le=180)
    max_characters: int = Field(default=3, ge=1, le=6)
    max_locations: int = Field(default=5, ge=1, le=10)
    ending_type: str = "cliffhanger"
```

## Character

```python
class Appearance(BaseModel):
    hair: str
    clothing: str
    distinctive_features: list[str]


class Character(BaseModel):
    id: str
    name: str
    role: str
    age: int | None
    personality: list[str]
    goal: str
    ability: str | None
    appearance: Appearance
    visual_prompt_zh: str
    visual_prompt_en: str
```

## StoryPackage

```python
class PlotBeat(BaseModel):
    beat_type: str
    duration: int
    content: str


class StoryPackage(BaseModel):
    title: str
    logline: str
    worldview: dict
    characters: list[Character]
    beats: list[PlotBeat]
    story_text: str
```

## ScreenplayPackage

```python
class Dialogue(BaseModel):
    speaker_id: str
    text: str
    emotion: str | None = None


class Scene(BaseModel):
    scene_id: str
    location_id: str
    duration: int
    characters: list[str]
    action: str
    dialogues: list[Dialogue]
    voiceover: str | None
    emotion: str
    transition: str | None


class ScreenplayPackage(BaseModel):
    title: str
    target_duration: int
    characters: list[Character]
    locations: list[dict]
    scenes: list[Scene]
```

## ShotPackage

```python
class Shot(BaseModel):
    shot_id: str
    scene_id: str
    duration: int
    shot_type: str
    camera_movement: str
    visual_description: str
    character_ids: list[str]
    image_prompt: str
    video_prompt: str
    negative_prompt: str | None
    audio_prompt: str | None
```

---

# 七、LangGraph State设计

不要把所有数据塞进一个巨大的字符串。

```python
from typing import TypedDict


class MangaGraphState(TypedDict, total=False):
    project_id: str
    user_input: str

    brief: ProjectBrief
    worldview: dict
    characters: list[Character]
    plot_beats: list[PlotBeat]
    story_package: StoryPackage

    story_review: dict
    story_revision_count: int

    screenplay: ScreenplayPackage
    screenplay_review: dict
    screenplay_revision_count: int

    shots: list[Shot]

    media_tasks: list[dict]
    output_video_path: str

    errors: list[str]
```

LangGraph负责全局状态和流程跳转；LangChain可以用于模型接入、Prompt、结构化输出和工具封装。官方文档也将 LangChain定位为模型和工具集成层，将 LangGraph定位为持久化、流式执行和状态编排层。([Docs by LangChain][3])

---

# 八、LangGraph主流程示例

```python
from langgraph.graph import StateGraph, START, END


def build_graph():
    graph = StateGraph(MangaGraphState)

    graph.add_node("requirements", requirement_node)
    graph.add_node("world_builder", world_builder_node)
    graph.add_node("character_designer", character_node)
    graph.add_node("plot_planner", plot_node)
    graph.add_node("story_writer", story_writer_node)
    graph.add_node("story_reviewer", story_reviewer_node)
    graph.add_node("story_reviser", story_reviser_node)

    graph.add_node("screenplay_adapter", screenplay_adapter_node)
    graph.add_node("visual_reviewer", visual_reviewer_node)
    graph.add_node("screenplay_reviser", screenplay_reviser_node)

    graph.add_node("penshot", penshot_node)
    graph.add_node("media_generator", media_generator_node)
    graph.add_node("composer", compose_node)

    graph.add_edge(START, "requirements")
    graph.add_edge("requirements", "world_builder")
    graph.add_edge("world_builder", "character_designer")
    graph.add_edge("character_designer", "plot_planner")
    graph.add_edge("plot_planner", "story_writer")
    graph.add_edge("story_writer", "story_reviewer")

    graph.add_conditional_edges(
        "story_reviewer",
        route_story_review,
        {
            "revise": "story_reviser",
            "continue": "screenplay_adapter",
        },
    )

    graph.add_edge("story_reviser", "story_reviewer")
    graph.add_edge("screenplay_adapter", "visual_reviewer")

    graph.add_conditional_edges(
        "visual_reviewer",
        route_screenplay_review,
        {
            "revise": "screenplay_reviser",
            "continue": "penshot",
        },
    )

    graph.add_edge("screenplay_reviser", "visual_reviewer")
    graph.add_edge("penshot", "media_generator")
    graph.add_edge("media_generator", "composer")
    graph.add_edge("composer", END)

    return graph.compile()
```

返工条件：

```python
def route_story_review(state: MangaGraphState) -> str:
    review = state["story_review"]
    count = state.get("story_revision_count", 0)

    if review["passed"]:
        return "continue"

    if count >= 2:
        return "continue"

    return "revise"
```

即使审核始终不通过，也不能无限循环。

---

# 九、如何接入 PenShot

PenShot支持三种适合你们的方式：

1. Python SDK
2. REST API
3. LangGraph节点

它还支持 MCP 和 A2A，但课程项目没有必要引入。([GitHub][2])

## 最推荐：Python SDK

安装：

```bash
pip install penshot
```

PenShot当前文档给出的包名为 `penshot`，仓库名是 `story-shot-agent`。([GitHub][4])

你们封装一个适配器：

```python
class PenShotAdapter:
    def __init__(self, client):
        self.client = client

    def generate_storyboard(
        self,
        screenplay: ScreenplayPackage,
    ) -> list[Shot]:
        script_text = self._to_script_text(screenplay)

        result = self.client.generate(
            script=script_text,
            target_model="kling",
        )

        return self._normalize_output(result)

    def _to_script_text(self, screenplay: ScreenplayPackage) -> str:
        sections = []

        for scene in screenplay.scenes:
            sections.append(
                f"""
场景：{scene.scene_id}
时长：{scene.duration}秒
动作：{scene.action}
旁白：{scene.voiceover or ""}
对白：
{self._format_dialogues(scene.dialogues)}
"""
            )

        return "\n".join(sections)
```

这里最重要的是 `_normalize_output()`。

无论 PenShot 返回什么格式，最后都统一转换成你们自己的 `Shot` 模型。

这样未来替换 PenShot也不会影响上层。

---

# 十、视频 Provider 设计

你们需要设计统一接口，而不是在业务代码里直接写某个厂商API。

```python
from abc import ABC, abstractmethod


class VideoProvider(ABC):
    @abstractmethod
    async def submit(
        self,
        prompt: str,
        duration: int,
        reference_image: str | None = None,
    ) -> str:
        """提交视频任务，返回任务ID。"""

    @abstractmethod
    async def get_status(self, task_id: str) -> str:
        """返回 pending、running、success 或 failed。"""

    @abstractmethod
    async def get_result(self, task_id: str) -> str:
        """返回生成视频的下载地址。"""
```

具体实现：

```python
class KlingVideoProvider(VideoProvider):
    ...


class VolcengineVideoProvider(VideoProvider):
    ...


class MockVideoProvider(VideoProvider):
    ...
```

一定要做 `MockVideoProvider`。

开发过程中不要每次调用真实视频API。

```python
class MockVideoProvider(VideoProvider):
    async def submit(self, prompt, duration, reference_image=None):
        return "mock-task-001"

    async def get_status(self, task_id):
        return "success"

    async def get_result(self, task_id):
        return "tests/assets/mock_video.mp4"
```

---

# 十一、角色一致性的实现

角色一致性是这个项目最大的实际难点。

建议采用三层约束。

## 第一层：角色卡锁定

每个角色固定：

```text
年龄
发型
发色
服装
主色调
脸部特征
标志性物品
```

后面的每一个Prompt都从角色卡自动注入这些描述。

---

## 第二层：参考图

先生成角色三视图或角色标准图。

```text
正面
侧面
半身
全身
表情
```

之后所有图片或视频尽量使用该参考图。

PenShot当前也包含角色三视图提示词生成和跨镜头连续性机制，可以作为这一层的基础。([GitHub][2])

---

## 第三层：Prompt模板固定

不要让每个Agent自由描述同一个角色。

错误：

```text
镜头1：黑衣少年
镜头2：年轻剑客
镜头3：英俊的修仙者
```

正确：

```text
CHARACTER_LIN_CHEN =
18-year-old Chinese male,
short black hair,
gray cultivation robe,
small scar below left eye
```

每次生成：

```python
prompt = f"""
{CHARACTER_LIN_CHEN}

Scene:
{shot.visual_description}

Camera:
{shot.shot_type}, {shot.camera_movement}
"""
```

---

# 十二、前端页面设计

建议使用 Streamlit，因为只有两周。

## 页面1：创建项目

字段：

```text
作品名称
小说类型
叙事风格
主角姓名
主角性格
核心脑洞
目标时长
画面风格
结尾类型
```

---

## 页面2：故事规划

展示：

```text
世界观
人物卡
剧情大纲
```

允许用户：

* 重新生成
* 手动修改
* 确认进入下一步

---

## 页面3：完整故事

展示故事文本。

按钮：

```text
重新生成
要求修改
确认改编为漫剧
```

---

## 页面4：漫剧剧本

表格：

| 场景 | 时长 | 角色 | 画面     | 对白       | 旁白   |
| -- | -: | -- | ------ | -------- | ---- |
| 1  | 8秒 | 林辰 | 妖兽冲向林辰 | 无        | 死亡降临 |
| 2  | 6秒 | 林辰 | 时间突然倒流 | 林辰：怎么回事？ | 无    |

---

## 页面5：分镜

每个镜头卡片显示：

```text
镜头编号
时长
景别
运镜
角色
视觉描述
图片Prompt
视频Prompt
```

---

## 页面6：视频任务

显示：

```text
镜头1：生成完成
镜头2：生成中
镜头3：等待
镜头4：失败，可重试
```

---

# 十三、推荐项目目录

```text
storymotion/
├── app/
│   ├── api/
│   │   ├── projects.py
│   │   ├── generation.py
│   │   └── media.py
│   │
│   ├── graphs/
│   │   ├── story_graph.py
│   │   ├── screenplay_graph.py
│   │   ├── media_graph.py
│   │   └── main_graph.py
│   │
│   ├── agents/
│   │   ├── requirement_agent.py
│   │   ├── world_builder_agent.py
│   │   ├── character_agent.py
│   │   ├── plot_agent.py
│   │   ├── story_writer_agent.py
│   │   ├── consistency_agent.py
│   │   ├── screenplay_adapter_agent.py
│   │   └── visual_reviewer_agent.py
│   │
│   ├── adapters/
│   │   └── penshot_adapter.py
│   │
│   ├── providers/
│   │   ├── base.py
│   │   ├── image/
│   │   │   ├── mock.py
│   │   │   └── provider_a.py
│   │   ├── video/
│   │   │   ├── mock.py
│     │   └── provider_a.py
│   │   └── tts/
│   │       └── provider_a.py
│   │
│   ├── models/
│   │   ├── project.py
│   │   ├── character.py
│   │   ├── story.py
│   │   ├── screenplay.py
│   │   └── shot.py
│   │
│   ├── prompts/
│   │   ├── requirements.txt
│   │   ├── world_builder.txt
│   │   ├── character.txt
│   │   ├── plot.txt
│   │   ├── writer.txt
│   │   ├── reviewer.txt
│   │   └── screenplay_adapter.txt
│   │
│   ├── services/
│   │   ├── llm_service.py
│   │   ├── task_service.py
│   │   ├── storage_service.py
│   │   └── ffmpeg_service.py
│   │
│   └── main.py
│
├── frontend/
│   └── streamlit_app.py
│
├── tests/
│   ├── test_story_graph.py
│   ├── test_screenplay_adapter.py
│   ├── test_penshot_adapter.py
│   └── fixtures/
│
├── data/
├── outputs/
├── pyproject.toml
├── .env.example
├── docker-compose.yml
└── README.md
```

---

# 十四、四个人分工

## 成员A：故事创作系统

负责：

* Requirement Agent
* World Builder
* Character Agent
* Plot Planner
* Story Writer
* Story Reviewer

交付：

```text
用户灵感 → StoryPackage
```

---

## 成员B：漫剧改编与数据协议

负责：

* 所有 Pydantic 数据模型
* Screenplay Adapter
* 时长规划
* 对白与旁白生成
* Visualizability Reviewer
* StoryPackage → ScreenplayPackage

这是整个项目最核心的接口负责人。

---

## 成员C：PenShot和视频生成

负责：

* PenShot部署和调用
* PenShot输出适配
* Image Provider
* Video Provider
* 异步任务轮询
* FFmpeg拼接

交付：

```text
ScreenplayPackage → ShotPackage → MP4
```

---

## 成员D：前端、后端接口和集成

负责：

* FastAPI
* Streamlit
* SQLite
* 项目状态展示
* 日志
* Demo
* Docker和README

交付：

```text
用户可操作的完整产品
```

---

# 十五、两周开发计划

## 第1天：技术验证

四个人共同完成：

* 跑通 NovelWriter
* 跑通 PenShot示例
* 跑通一个LLM结构化输出
* 确认视频厂商API可用
* 确定Python版本

当天必须回答：

```text
PenShot是否真的能处理中文剧本？
输出JSON长什么样？
视频API是否已经拿到Key？
生成一个视频需要多久？
```

---

## 第2天：定义数据协议

完成：

* ProjectBrief
* Character
* StoryPackage
* ScreenplayPackage
* ShotPackage

这一天不要急着写 Agent。

数据协议不稳定，后面所有人的代码都会互相冲突。

---

## 第3—4天：跑通故事主链

先完成：

```text
需求解析
→ 世界观
→ 人物
→ 大纲
→ 故事
```

不用审核循环。

---

## 第5天：故事审核和返工

增加：

```text
Story Reviewer
→ Revision
→ Reviewer
```

最多两轮。

---

## 第6—7天：漫剧改编

完成：

```text
StoryPackage
→ ScreenplayPackage
```

同时加入：

* 总时长限制
* 场景数量限制
* 人物数量限制
* 对白长度限制
* 可视化检查

第一周结束时必须能输出完整结构化剧本。

---

## 第8天：接入 PenShot

完成：

```text
ScreenplayPackage
→ PenShot
→ ShotPackage
```

不要修改 PenShot内部源码，先写 Adapter。

---

## 第9天：图片和视频 Provider

只接一家。

完成：

```text
Shot
→ 图片
→ 视频任务
→ 任务状态
→ 视频下载
```

同时完成 Mock Provider。

---

## 第10天：FFmpeg合成

功能：

* 视频片段排序
* 简单转场
* 音轨合并
* 字幕可选
* 输出MP4

---

## 第11—12天：前端

完成六个页面：

```text
输入
故事设定
故事正文
漫剧剧本
分镜
视频结果
```

---

## 第13天：完整联调

固定一个Demo案例：

```text
东方玄幻
主角林辰
每天可以倒退十秒
目标60秒
悬念结尾
```

将所有随机性尽量降低。

保存一次已经成功生成的视频，真实演示失败时可以回退展示。

---

## 第14天：答辩材料

准备：

* 架构图
* LangGraph执行图
* Agent职责表
* 数据流图
* 普通LLM和Multi-Agent对比
* 失败案例
* 成本和时延
* Demo录像

---

# 十六、两周内必须缩减的功能

不要做：

* 长篇小说
* 连续几十章生成
* 多用户权限
* 社区发布
* 复杂时间线编辑器
* 同时接入多家视频模型
* 自动训练模型
* 自建图像或视频模型
* 完整商业级任务队列
* 实时多人协作
* 自动生成十分钟视频

第一版限制：

```text
故事长度：500—1000字
视频长度：30—90秒
主要角色：2—3人
场景数量：3—6个
镜头数量：5—10个
视频Provider：1家
图片Provider：1家
```

---

# 十七、主要风险

## 风险1：PenShot输出与你们预想不同

解决办法：

> 所有 PenShot结果必须经过 Adapter 转成你们自己的 ShotPackage。

不要让前端直接依赖 PenShot原始返回值。

---

## 风险2：视频API太慢或失败

解决办法：

* 异步任务
* 每个镜头可单独重试
* Mock Provider
* 缓存生成结果
* Demo准备预生成成片
* 失败时展示分镜和Prompt

---

## 风险3：故事太长，视频装不下

解决办法：

在 Plot Planner阶段就分配时间：

```text
Hook：5秒
Setup：10秒
Conflict：20秒
Reversal：15秒
Ending：10秒
```

不要写完故事以后才压缩。

---

## 风险4：角色长得不一样

解决办法：

* 固定角色卡
* 角色参考图
* 固定视觉Prompt
* 视频优先使用图生视频
* 每个镜头注入同一角色描述
* 减少换装
* 减少复杂镜头

---

## 风险5：NovelWriter改造范围过大

解决办法：

不要把整个 NovelWriter代码引入主项目。

优先借鉴：

```text
世界观生成步骤
人物生成步骤
故事结构规划
质量审核机制
反馈循环
```

NovelWriter本身强调现有应用与 Agent层分离、明确接口和反馈循环，这些设计理念很适合借鉴；但其 Tkinter UI、文件管理和长篇章节系统不适合直接搬进两周MVP。([GitHub][1])

---

# 十八、项目原创性怎么体现

老师可能会问：

> 你们是不是只是拼接开源项目？

你们应当明确回答：

### 借鉴部分

* NovelWriter的故事规划思想
* PenShot的剧本分镜组件

### 自研部分

* 统一 LangGraph总工作流
* 用户灵感结构化
* 短漫剧专用剧情规划
* StoryPackage数据协议
* 小说到漫剧剧本的改编 Agent
* 视频时长和场景约束
* PenShot Adapter
* 视频厂商 Provider接口
* 人工确认节点
* 整体前端和项目管理

其中最能体现原创性的，是：

> **从小说叙事文本到可生成漫剧的视频剧本转换。**

这不是简单调用接口，而是一个有明确输入、约束、评价标准和返工循环的 Agent 工作流。

---

# 十九、最终建议

方案二最好落地成：

```text
NovelWriter：
只借鉴故事规划、角色生成和审核思路

你们自研：
LangGraph故事系统
小说到漫剧改编层
统一数据协议
Provider层
产品界面

PenShot：
作为剧本到分镜的Python依赖使用
```

推荐技术栈：

```text
Python 3.11
FastAPI
LangGraph
LangChain
Pydantic
PenShot
SQLite
Chroma，可选
Streamlit
FFmpeg
```

最终核心链路：

```text
灵感输入
→ ProjectBrief
→ StoryPackage
→ ScreenplayPackage
→ PenShot
→ ShotPackage
→ VideoProvider
→ MP4
```

按照这个范围，四个人两周完成一个30—60秒漫剧Demo的可行性较高。最关键的三个里程碑是：

1. **第4天前生成稳定的 StoryPackage。**
2. **第7天前生成稳定的 ScreenplayPackage。**
3. **第10天前至少生成并拼接两个真实视频片段。**

只要这三个节点按时完成，后面基本就是界面、调试和演示优化。

[1]: https://github.com/EdwardAThomson/NovelWriter "GitHub - EdwardAThomson/NovelWriter: An app that assists authors in writing novels / short stories by leveraging Large Language Models (LLMs). · GitHub"
[2]: https://github.com/neopen/story-shot-agent "GitHub - neopen/story-shot-agent: 剧本分镜工具智能体（PenShot）：电影/动漫/短剧/小说/剧本→分镜→片段→prompt | 基于 LangGraph+LLM，自动解析任意格式剧本，生成 Sora/Veo/Runway 等模型可用的连贯text-to-video提示词。保持角色/剧情跨片段一致，支持 MCP/REST API/函数调用 | Python库 + A2A集成。（LLM-powered screenplay-to-video-prompt agent. Break scripts into Sora/Veo-ready shots with character consistency） · GitHub"
[3]: https://docs.langchain.com/oss/python/langgraph/overview "LangGraph overview - Docs by LangChain"
[4]: https://github.com/neopen/story-shot-agent/blob/main/docs/README-en.md "story-shot-agent/docs/README-en.md at main · neopen/story-shot-agent · GitHub"
